# SmartMoneyScore V1 Independent Validation

## Objective

Validate that SmartMoneyScore V1 is deterministic, explainable, bounded and safe
for production orchestration.

## Scope

In scope:

- V1 metadata/storage;
- pure scoring behavior;
- full historical initload;
- factor/score persistence;
- public view;
- incremental convergence;
- missing LimitUp semantics.

Out of scope:

- As-Traded market-limit migration itself;
- sector aggregation;
- predictive calibration;
- UI.

## Production files

~~~text
src/calcEngine/smartMoneyScore.py
src/cherrystock/infrastructure/database/repositories/smart_money_repository.py
src/cherrystock/infrastructure/database/unit_of_work.py
src/DuckDB/sql/smart_money_v1_schema.sql
src/DuckDB/sql/smart_money_v1_preflight.sql
scripts/initload/init_reload_smart_money_score.py
scripts/run_smart_money.py
scripts/validate_smart_money_incremental.py
~~~

## Test files

~~~text
tests/test_smart_money_score.py
tests/test_smart_money_evaluation.py
tests/test_smart_money_integration.py
tests/test_smart_money_score.md
~~~

## Preconditions

1. OBV/AD deployment PASS.
2. vw_Ticker_OHLC_D exists.
3. raw_index_eod contains VNINDEX.
4. Indicator Engine public views exist.
5. local CherryMon DuckDB is writable.
6. As-Traded market limit may be absent; this is expected.

## Test cases

### T1 — Focused unit tests

~~~powershell
python -m pytest tests/test_smart_money_score.py tests/test_smart_money_evaluation.py tests/test_smart_money_integration.py -v
~~~

Expected: all tests PASS.

### T2 — Full historical initload

~~~powershell
python scripts\initload\init_reload_smart_money_score.py
~~~

Expected: transaction commits; score/factor rows > 0.

### T3 — SQL preflight

Execute:

~~~text
src/DuckDB/sql/smart_money_v1_preflight.sql
~~~

Expected: object, key, range, NULL semantics and public-view checks pass.

### T4 — Incremental convergence

~~~powershell
python scripts\validate_smart_money_incremental.py --days 30 --tickers MWG FPT HPG
~~~

Expected:

~~~text
SmartMoney full/incremental convergence: PASS
~~~

The script must rollback its validation transaction.

### T5 — State semantics

Inspect representative DISTRIBUTION, SUPPLY_LOCK, BREAKOUT, ACCUMULATION and
NEUTRAL rows.

Expected:

- Distribution is a penalty;
- SupplyLock is not generated by liquidity compression alone;
- missing LimitUp is not negative evidence;
- score remains 0..100.

### T6 — Public contract

Query vw_Ticker_SmartMoney.

Expected:

- one enabled-model row per Ticker+Date;
- component scores visible;
- model identity visible;
- ConfidenceScore independently visible.

### T7 — OOS evaluation execution

Action:

~~~powershell
python scripts\evaluate_smart_money_v1.py --horizons 5 10 20
~~~

Expected:

- TRAIN/VALIDATION/TEST evidence exists when history is sufficient;
- metrics.csv / monotonicity.csv / summary.json are produced;
- evaluator performs no database writes;
- TEST metrics are reported separately from TRAIN.

Predictive quality is reported, not auto-approved.

## Failure handling

One focused repair is allowed for a current-change failure, then rerun only the
failed focused test. Do not change unrelated market/indicator code.

## Stop condition

Stop after T1–T7 have a terminal decision.

## Result format

~~~text
SmartMoneyScore V1 Validation
-----------------------------
T1 Unit: PASS | FAIL | BLOCKED
T2 Full init: PASS | FAIL | BLOCKED
T3 Preflight: PASS | FAIL
T4 Convergence: PASS | FAIL
T5 State semantics: PASS | WARNING | FAIL
T6 Public contract: PASS | FAIL
T7 OOS evaluation: PASS | WARNING | FAIL

Verdict: PASS | FAIL | BLOCKED | REGRESSION
Action: KEEP | FIX ONCE | REVERT | STOP
~~~


### CI / synthetic integration evidence — 2026-09-06

GitHub Actions workflow:

```text
.github/workflows/smart-money-validation.yml
```

Validated on Python 3.13 / DuckDB 1.5.5:

```text
Run:        34027453182
Run number: 4
Commit:     636b2299c43c2d108d03c29fd9a6e3993c8e59d0
Conclusion: SUCCESS
Tests:      12 passed
```

Coverage includes:

- module/runner compilation;
- pure scoring/unit semantics;
- OOS evaluation semantics;
- real SmartMoney schema bootstrap in synthetic DuckDB;
- full historical refresh;
- public `vw_Ticker_SmartMoney`;
- missing LimitUp = NULL / UNAVAILABLE;
- incremental checkpoint refresh;
- full-history vs incremental factor/score convergence.

The integration gate found and forced repair of two implementation defects before
PASS:

1. DuckDB 1.5.5 did not accept `CURRENT_TIMESTAMP` in the SmartMoney
   `ON CONFLICT ... DO UPDATE` expression as written. Upserts now reuse
   `EXCLUDED.UpdatedAt`.
2. A single 70-session incremental warmup was insufficient for exact memory
   convergence because early warmup rows themselves lacked mature 60-session
   features. Incremental execution now separates:
   `feature_start -> memory_start -> target_start`.

This CI evidence validates code/schema behavior on a deterministic fixture. It does
not replace the TestEngineer run against the user's production/local CherryMon
database.
